@{
    Layout = "_Layout";
}
<div class="container-fluid flex-grow-1 container-p-y">
    <div class="row">
        <h4 class="font-weight-bold py-3 mb-4">
            Deducciones
        </h4>
    </div>
    <div class="row">
        <div class="col-12">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>First Name</th>
                        <th>Last Name</th>
                    </tr>
                </thead>
                <tbody>
                    @if (this.ViewBag.Data.Count > 0)
                    {
                        @foreach (var record in this.ViewBag.Data)
                        {
                            <tr>
                                <th scope="row">@record.Id</th>
                                <td>@record.Nombre</td>
                                <td>@record.FechaCreacion</td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <th colspan="5" scope="row">No se encontraro registros</th>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
